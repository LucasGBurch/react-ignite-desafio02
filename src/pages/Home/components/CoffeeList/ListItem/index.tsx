import { ListItemContainer } from './styles';

interface ListItemProps {
  coffeeImg: string;
  tipo1: string;
  tipo2?: string;
  tipo3?: string;
}

export function ListItem({ coffeeImg, tipo1, tipo2, tipo3 }: ListItemProps) {
  return <ListItemContainer>
    
  </ListItemContainer>;
}
